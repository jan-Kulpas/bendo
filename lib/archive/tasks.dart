// import 'package:bendo/models/task.dart';
// import 'package:flutter/material.dart';

// class Tasks with ChangeNotifier {
//   final Map<String, Task> _items = {};

//   Map<String, Task> get items {
//     return {..._items};
//   }

//   void addTask(Task task) {
//     _items[task.id] = task;
//   }

//   void removeTask(String id) {
//     _items.remove(_items[id]);
//   }
// }

// ? i dunno maybe a better approach for now is to store the tasks
// ? in the collections themselves instead of messing with id's and all that
// ? Come back once you have a solution for layered tasks or make a search function
